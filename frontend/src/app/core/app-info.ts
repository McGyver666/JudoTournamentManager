/**
 * Static application build metadata surfaced in the shell footer.
 * Kept as a single maintained constant so the offline/LAN build has no
 * runtime dependency on a version endpoint.
 */
export const APP_VERSION = '0.9.0-mvp';
